# pnpm + 打包工具（electron-builder/forge）更兼容
node-linker=hoisted

# npm registry（可选：不影响 Electron 二进制，但建议别用奇怪的源）
registry=https://registry.npmjs.org/

# Electron 二进制镜像（关键）
electron_mirror=https://npmmirror.com/mirrors/electron/
# 有些镜像/版本校验需要用远端校验文件（可选，但常能救命）
electron_use_remote_checksums=1